var ZMD_COMMON = function(e, r) {
    return r.parseURI = function() {
        var r = e.location.href,
            a = document.createElement("a");
        return a.href = r, {
            source: r,
            protocol: a.protocol.replace(":", ""),
            host: a.hostname,
            port: a.port,
            query: a.search,
            params: function() {
                for (var e, r = {}, t = a.search.replace(/^\?/, "").split("&"), n = t.length, o = 0; o < n; o++) 
                    t[o] && (e = t[o].split("="), r[e[0]] = e[1]);
                return r
            }(),
            file: (a.pathname.match(/\/([^\/?#]+)$/i) || [, ""])[1],
            hash: a.hash.replace("#", ""),
            path: a.pathname.replace(/^([^\/])/, "/$1"),
            relative: (a.href.match(/tps?:\/\/[^\/]+(.+)/) || [, ""])[1],
            segments: a.pathname.replace(/^\//, "").split("/")
        }
    },
    r
}(window, window.ZMD_COMMON = window.ZMD_COMMON || {});
